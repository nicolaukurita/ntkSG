import { Component } from '@angular/core';

@Component({
    selector: '${projeto.nome}-root',
    templateUrl: '.app.component.html'
})
export class AppComponent {
    pageTitle: string = '${projeto.titulo}';
}